version: '3.3'
 
## Nginx Service 
services: 
 webserver:
    image: nginx:alpine
    container_name: srv-ws-nginx
    restart: unless-stopped
    tty: true
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./conf/nginx/conf.d/:/etc/nginx/conf.d